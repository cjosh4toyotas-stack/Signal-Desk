diff --git a/alpaca-proxy-worker.js b/alpaca-proxy-worker.js
index 5121988..a7ff77d 100644
--- a/alpaca-proxy-worker.js
+++ b/alpaca-proxy-worker.js
@@ -637,7 +637,10 @@ async function mergeSched13History(env, records) {
     if (!rec || !rec.acc) continue;
     const prev = byAcc.get(rec.acc);
     if (!prev) byAcc.set(rec.acc, rec);
-    else if (rec.tickers && rec.tickers.length && !(prev.tickers || []).length) prev.tickers = rec.tickers;
+    else {
+      if (rec.tickers && rec.tickers.length && !(prev.tickers || []).length) prev.tickers = rec.tickers;
+      if (rec.kind && !prev.kind) { prev.kind = rec.kind; prev.flags = rec.flags; prev.pct = rec.pct; }
+    }
   }
   const cutoff = Date.now() - SCHED13_MAX_AGE_DAYS * 86400e3;
   const merged = [...byAcc.values()]
@@ -684,8 +687,82 @@ async function handleSched13History(env) {
   });
 }
 
+// ── WHAT KIND OF 13D — mirrors classify13D() in index.html ────
+// A 13D whose shares came from a securities purchase agreement /
+// convertible / warrant package (typically with a 9.99% ownership blocker)
+// is the COMPANY selling paper to the filer — dilution, not accumulation.
+// Those never push an alert and are stored with kind='financing' so the
+// dashboard demotes them without re-reading EDGAR. Keep this function
+// byte-for-byte in sync with the frontend copy.
+function classify13D(text) {
+  const t = String(text || '');
+  const count = re => (t.match(re) || []).length;
+  const flags = [];
+  const financingHits =
+    count(/securities purchase agreement/gi) + count(/subscription agreement/gi) + count(/private placement/gi) +
+    count(/\bPIPE\b/g) + count(/convertible (?:note|notes|debenture|debentures|preferred)/gi) + count(/\bdebentures?\b/gi) +
+    count(/purchase warrants?\b/gi) + count(/registration rights agreement/gi) + count(/additional investment right/gi) +
+    count(/conversion price/gi) + count(/stated value/gi) + count(/exercise price/gi);
+  const blocker = /beneficial ownership (?:limitation|restriction|cap|blocker)/i.test(t) || /\b[49]\.99\s*%/.test(t) || /ownership (?:limitation|restriction) of [49]\.99/i.test(t);
+  if (blocker) flags.push('9.99%-style ownership blocker');
+  if (/securities purchase agreement|subscription agreement|private placement/i.test(t)) flags.push('shares issued by the company (SPA / private placement)');
+  if (/convertible|debenture/i.test(t)) flags.push('convertible security');
+  if (/\bwarrants?\b/i.test(t) && financingHits >= 2) flags.push('warrants');
+  if (/additional investment right/i.test(t)) flags.push('further-investment right');
+  const dealHits = count(/voting agreement/gi) + count(/support agreement/gi) + count(/agreement and plan of merger/gi) + count(/merger agreement/gi) + count(/tender offer/gi);
+  const intentHits = count(/nominat(?:e|ion|ing|ed)/gi) + count(/proxy (?:contest|solicitation|fight|statement)/gi) +
+    count(/strategic (?:alternatives|review)/gi) + count(/sale of the (?:issuer|company)/gi) + count(/board (?:composition|representation|refresh|seats?)/gi) +
+    count(/letter to the (?:board|issuer|company)/gi) + count(/maximiz(?:e|ing) (?:shareholder|stockholder) value/gi) + count(/undervalued/gi) +
+    count(/unsolicited/gi) + count(/proposal to acquire/gi) + count(/going.private/gi) + count(/special meeting/gi);
+  const openMarketHits = count(/open[- ]market/gi);
+  const affiliate = /(?:director|officer|chairman|chief executive officer|founder|president) of the (?:issuer|company)/i.test(t) ||
+    /serves? as (?:a |the )?(?:director|chairman|chief executive)/i.test(t) || /employment agreement/i.test(t);
+  if (affiliate) flags.push('filer is a director/officer/founder of the issuer');
+  if (dealHits) flags.push('voting/merger agreement');
+  if (intentHits >= 2) flags.push('stated activist intent');
+  if (openMarketHits) flags.push('open-market purchases');
+  const strongFinancing = /securities purchase agreement|subscription agreement|private placement/i.test(t) && /convertible|debenture|warrant|preferred/i.test(t);
+  let kind = 'plain';
+  if (strongFinancing || financingHits >= 4 || (blocker && financingHits >= 1)) kind = 'financing';
+  else if (dealHits >= 2) kind = 'deal';
+  else if (intentHits >= 2) kind = 'activist';
+  else if (openMarketHits >= 1 && !affiliate) kind = 'accumulation';
+  else if (affiliate || blocker) kind = 'affiliate';
+  return { kind, flags, blocker, affiliate, openMarket: openMarketHits > 0, intent: intentHits >= 2 };
+}
+
+function parse13DPercent(text) {
+  const tagHits = [...text.matchAll(/<[^>/]*[Pp]ercent[^>]*>\s*([\d.]+)/g)]
+    .map(x => parseFloat(x[1])).filter(v => v > 0 && v <= 100);
+  if (tagHits.length) return Math.max(...tagHits);
+  const pm = text.match(/[Pp]ercent of class[\s\S]{0,300}?([\d.]+)\s*%/);
+  return pm ? parseFloat(pm[1]) : null;
+}
+
+// Read one 13D's full submission text and classify it. Cached in KV per
+// accession so each filing is read at most once across scans.
+async function readSched13(env, rec) {
+  const m = (rec.link || '').match(/\/data\/(\d+)\/(\d{18})\//);
+  if (!m) return null;
+  const key = 'sched13-read:' + rec.acc;
+  if (env.SIGNAL_KV) {
+    try { const c = await env.SIGNAL_KV.get(key, 'json'); if (c) return c; } catch { /* fall through */ }
+  }
+  const dashed = `${m[2].slice(0, 10)}-${m[2].slice(10, 12)}-${m[2].slice(12)}`;
+  const url = `https://www.sec.gov/Archives/edgar/data/${parseInt(m[1], 10)}/${m[2]}/${dashed}.txt`;
+  const res = await fetch(url, { headers: { 'User-Agent': SEC_USER_AGENT } });
+  if (!res.ok) throw new Error('EDGAR ' + res.status + ' reading ' + rec.acc);
+  const text = await res.text();
+  const cls = classify13D(text);
+  const out = { kind: cls.kind, flags: cls.flags, pct: parse13DPercent(text), at: new Date().toISOString() };
+  if (env.SIGNAL_KV) {
+    try { await env.SIGNAL_KV.put(key, JSON.stringify(out), { expirationTtl: 60 * 86400 }); } catch { /* non-fatal */ }
+  }
+  return out;
+}
+
 async function runAlertScan(env) {
-  const status = { ran: new Date().toISOString(), scanned: 0, alerted: 0, errors: [] };
+  const status = { ran: new Date().toISOString(), scanned: 0, alerted: 0, suppressedFinancing: 0, errors: [] };
   try {
     const feedUrl = 'https://www.sec.gov/cgi-bin/browse-edgar?action=getcurrent&type=SCHEDULE+13D&company=&dateb=&owner=include&count=100&output=atom';
     const res = await fetch(feedUrl, { headers: { 'User-Agent': SEC_USER_AGENT, 'Accept': 'application/atom+xml' } });
@@ -713,8 +790,33 @@ async function runAlertScan(env) {
       if (form) rec.form = form;
       byAcc.set(acc, rec);
     }
+    // EDGAR no longer honors the type= filter on getcurrent (verified
+    // 2026-08-28: the "SCHEDULE 13D" feed is ~95% 424B2/497K junk). Keep
+    // only real 13D/13G forms so junk never enters history or tiering.
+    for (const [acc, r] of byAcc) if (!/^SCHEDULE 13[DG]/.test(r.form)) byAcc.delete(acc);
     status.scanned = byAcc.size;
 
+    // Read every tier-2+ filing's text ONCE (KV-cached) and classify it
+    // before it can alert or enter history — a financing 13D is stored as
+    // such and never pushes. Capped per scan to respect the subrequest
+    // budget; anything left is picked up next run.
+    let reads = 0;
+    const readings = {};
+    for (const rec of byAcc.values()) {
+      const filerNames = rec.filers.join(' | ').toLowerCase();
+      const activist = ALERT_ACTIVIST_ROSTER.find(a => filerNames.includes(a)) || null;
+      const isNewD = rec.form === 'SCHEDULE 13D';
+      const isD = rec.form.startsWith('SCHEDULE 13D');
+      rec.tier = (isNewD && activist) ? 3 : (isNewD || (isD && activist)) ? 2 : isD ? 1 : 0;
+      rec.activist = activist;
+      if (rec.tier < 2 || reads >= 15) continue;
+      try {
+        const cached = env.SIGNAL_KV ? await env.SIGNAL_KV.get('sched13-read:' + rec.acc, 'json') : null;
+        if (!cached) reads++;
+        readings[rec.acc] = cached || await readSched13(env, rec);
+      } catch (err) { status.errors.push(err.message); }
+    }
+
     // Persist everything this scan saw into the rolling 13D history — this
     // is what lets the frontend's ★ Opportunities view accumulate 24/7
     // instead of only knowing whatever the live feed shows at page load.
@@ -722,18 +824,20 @@ async function runAlertScan(env) {
       await mergeSched13History(env, [...byAcc.values()].map(r => ({
         acc: r.acc, form: r.form, filed: r.filed, link: r.link,
         subject: r.subject, tickers: [], filers: r.filers,
+        kind: readings[r.acc] ? readings[r.acc].kind : undefined,
+        flags: readings[r.acc] ? readings[r.acc].flags : undefined,
+        pct: readings[r.acc] ? readings[r.acc].pct : undefined,
       })));
     } catch (err) {
       status.errors.push('history merge failed: ' + err.message);
     }
 
     for (const rec of byAcc.values()) {
-      const filerNames = rec.filers.join(' | ').toLowerCase();
-      const activist = ALERT_ACTIVIST_ROSTER.find(a => filerNames.includes(a)) || null;
+      const { tier, activist } = rec;
       const isNewD = rec.form === 'SCHEDULE 13D';
-      const isD = rec.form.startsWith('SCHEDULE 13D');
-      const tier = (isNewD && activist) ? 3 : (isNewD || (isD && activist)) ? 2 : isD ? 1 : 0;
       if (tier < 2) continue;
+      const reading = readings[rec.acc];
+      if (!reading) continue; // unread this scan (budget) — alert decision waits for the next run
 
       // Dedupe: alert each accession exactly once.
       if (env.SIGNAL_KV) {
@@ -742,12 +846,20 @@ async function runAlertScan(env) {
         await env.SIGNAL_KV.put('alerted:' + rec.acc, '1', { expirationTtl: 14 * 86400 });
       }
 
+      // The metric fix: a financing 13D is not an accumulation signal. It
+      // is recorded (history carries kind='financing') but never pushed.
+      if (reading.kind === 'financing') { status.suppressedFinancing++; continue; }
+
       status.alerted++;
       if (env.NTFY_TOPIC) {
+        const kindTag = reading.kind === 'activist' ? ' · stated activist intent'
+          : reading.kind === 'accumulation' ? ' · open-market buying'
+          : reading.kind === 'affiliate' ? ' · insider-affiliated filer'
+          : reading.kind === 'deal' ? ' · deal-linked' : '';
         const title = tier >= 3
           ? `★ ACTIVIST 13D: ${rec.subject || 'unknown target'}`
           : `13D ${isNewD ? 'filed' : 'amended'}: ${rec.subject || 'unknown target'}`;
-        const body = `${rec.form} — filed by ${rec.filers.join(', ') || 'unknown'}${activist ? ` (roster match: ${activist})` : ''}\n${rec.link}`;
+        const body = `${rec.form} — filed by ${rec.filers.join(', ') || 'unknown'}${activist ? ` (roster match: ${activist})` : ''}${reading.pct != null ? ` — ${reading.pct.toFixed(1)}% stake` : ''}${kindTag}\n${rec.link}`;
         try {
           await fetch('https://ntfy.sh/' + encodeURIComponent(env.NTFY_TOPIC), {
             method: 'POST',
diff --git a/index.html b/index.html
index 3d6e029..633220b 100644
--- a/index.html
+++ b/index.html
@@ -1497,14 +1497,22 @@ function syncInsiderSignals() {
     const isBuy = t.code === 'P';
     const firstBuy = isBuy && insiderFirstBuyFlag(t);
     const w = insiderRoleWeight(t);
+    // Stake change: the same $ buy means more from someone who just doubled
+    // their position than from someone topping up a huge one by 1%. From
+    // the Form 4's shares-owned-after field; unknown → neutral.
+    const priorShares = t.ownedAfter != null ? (isBuy ? t.ownedAfter - t.shares : t.ownedAfter + t.shares) : null;
+    const stakeChg = priorShares != null && priorShares >= 0 ? t.shares / Math.max(priorShares, 1) : null;
+    const stakeMult = stakeChg == null ? 1 : stakeChg >= 1 ? 1.35 : stakeChg >= 0.5 ? 1.2 : stakeChg >= 0.2 ? 1.1 : 1;
+    const stakeNote = stakeChg != null && stakeChg >= 0.2 && isBuy
+      ? (priorShares === 0 ? ' — a new position from zero' : ` — grew their stake ${stakeChg >= 1 ? (1 + stakeChg).toFixed(1) + '×' : '+' + (stakeChg * 100).toFixed(0) + '%'}`) : '';
     const sig = {
       source: 'insider', tag: isBuy ? (firstBuy ? 'FIRST INSIDER BUY' : 'INSIDER BUY') : 'INSIDER SELL',
       entity: `${insiderWho(t)}${t.symbol ? ' — ' + t.symbol : ''}`,
-      detail: `${insiderRole(t) || 'Insider'} ${isBuy ? 'bought' : 'sold'} ${fmtShares(t.shares)} shares of ${t.issuerName}${t.avgPrice ? ' @ $' + t.avgPrice.toFixed(2) : ''} on the open market${firstBuy ? ' — first purchase by this insider in the local record' : ''}${clusteredSymbols.has(t.symbol) && isBuy ? ' · part of a cluster' : ''}`,
+      detail: `${insiderRole(t) || 'Insider'} ${isBuy ? 'bought' : 'sold'} ${fmtShares(t.shares)} shares of ${t.issuerName}${t.avgPrice ? ' @ $' + t.avgPrice.toFixed(2) : ''} on the open market${stakeNote}${firstBuy ? ' — first purchase by this insider in the local record' : ''}${clusteredSymbols.has(t.symbol) && isBuy ? ' · part of a cluster' : ''}`,
       direction: isBuy ? 'buy' : 'sell',
       time: t.filed, link: t.link, ticker: t.symbol || null,
       valueUsd: t.value,
-      sortValue: t.value * (isBuy ? w : 1) * (firstBuy ? 1.25 : 1)
+      sortValue: t.value * (isBuy ? w * stakeMult : 1) * (firstBuy ? 1.25 : 1)
     };
     allSignals.push(sig);
     if (isBuy && t.value >= 1e6 && !clusteredSymbols.has(t.symbol)) {
@@ -2074,7 +2082,10 @@ function computeConfluence() {
     if (!(s.valueUsd > 0) || s.direction === 'sell') continue;
     const key = isRealTicker(s.ticker) ? 'T:' + s.ticker : null;
     if (!key) continue;
-    let pts = Math.max(0.5, Math.log10(s.valueUsd / 1e5)) * fresh(s.time, 7);
+    // Materiality here too (clamped tighter than Top Signals): a trade that
+    // is a real share of the company earns more confluence than the same
+    // dollars at a megacap.
+    let pts = Math.max(0.5, Math.log10(s.valueUsd / 1e5)) * materialityMult(s, 0.5, 2.5) * fresh(s.time, 7);
     let badge = null;
     if (s.cluster) { pts *= 1.8; badge = { cls: 'b-cluster', text: '👥 CLUSTER BUY' }; }
     else if (s.source === 'insider') badge = { cls: 'b-insider', text: '💼 INSIDER BUY' };
@@ -2091,16 +2102,32 @@ function computeConfluence() {
   // ticker-less feed records key by company name and merge below.
   for (const r of sched13Filings) {
     if (r.tier < 2) continue;
+    // WHAT KIND of 13D: the filing text is read (sched13Quality) before a
+    // card is earned. A 13D whose shares came from a securities purchase
+    // agreement / convertible / warrant is a FINANCING, not accumulation —
+    // it never earns points (it's dilution for existing holders, the
+    // opposite of the signal). Insider-affiliated filers and deal-linked
+    // voting-agreement 13Ds are discounted; stated activist intent and
+    // open-market buying are rewarded.
+    const q = sched13Quality(r);
+    if (q.mult <= 0) continue;
     const key = isRealTicker(r.ticker) ? 'T:' + r.ticker : 'N:' + normName(r.subject);
-    let pts = r.tier * 2.2 * fresh(r.filed, 10);
+    let pts = r.tier * 2.2 * q.mult * fresh(r.filed, 10);
     add(key, (isRealTicker(r.ticker) ? r.ticker : r.subject), 'sched13', pts,
-      `${r.form === 'SCHEDULE 13D' ? 'New 13D' : '13D amendment'} by ${r.filers.slice(0, 2).join(', ')}${r.pct ? ' — ' + r.pct.toFixed(1) + '% stake' : ''}${r.pctDelta != null ? ` (${r.pctDelta > 0 ? '+' : ''}${r.pctDelta.toFixed(1)}pt vs prior)` : ''}`,
+      `${r.form === 'SCHEDULE 13D' ? 'New 13D' : '13D amendment'} by ${r.filers.slice(0, 2).join(', ')}${r.pct ? ' — ' + r.pct.toFixed(1) + '% stake' : ''}${r.pctDelta != null ? ` (${r.pctDelta > 0 ? '+' : ''}${r.pctDelta.toFixed(1)}pt vs prior)` : ''}${q.note ? ' · ' + q.note : ''}`,
       r.link, (isRealTicker(r.ticker) ? r.ticker : null), r.filed);
     const entry = byKey[key];
     const badge = r.escalation ? { cls: 'b-escalation', text: '⚠ 13G→13D' }
-      : r.activist ? { cls: 'b-13d', text: '★ ACTIVIST 13D' }
+      : (r.activist || r.kind === 'activist') ? { cls: 'b-13d', text: '★ ACTIVIST 13D' }
+      : r.kind === 'accumulation' ? { cls: 'b-13d', text: '📈 OPEN-MKT 13D' }
+      : r.kind === 'deal' ? { cls: 'b-13d', text: '🤝 DEAL-LINKED 13D' }
+      : r.kind === 'affiliate' ? { cls: 'b-13d', text: '13D · AFFILIATE' }
       : { cls: 'b-13d', text: '13D' };
     if (!entry.badges.some(b => b.text === badge.text)) entry.badges.push(badge);
+    if (r.mcap > 0 && r.mcap < 50e6 && entry.details.length < 5) {
+      const d = `↳ ~${fmtCap(r.mcap)} market cap (from the filing's share count) — microcap, thin float`;
+      if (!entry.details.includes(d)) entry.details.push(d);
+    }
   }
 
   // Stream 4: campaigns in proxy-fight stage — the late-cycle confirmation.
@@ -2383,10 +2410,12 @@ async function loadSchedule13() {
               byAcc.set(h.acc, {
                 acc: h.acc, form: (h.form || '').toUpperCase(), filed: h.filed,
                 link: h.link, subject: h.subject || '', filers: h.filers || [],
-                ticker: (h.tickers && h.tickers[0]) || null, fromHistory: true
+                ticker: (h.tickers && h.tickers[0]) || null, fromHistory: true,
+                workerKind: h.kind || null, workerFlags: h.flags || null, workerPct: h.pct != null ? h.pct : null
               });
-            } else if (h.tickers && h.tickers.length && !existing.ticker) {
-              existing.ticker = h.tickers[0]; // efts records know the subject's ticker; the atom feed doesn't
+            } else {
+              if (h.tickers && h.tickers.length && !existing.ticker) existing.ticker = h.tickers[0]; // efts records know the subject's ticker; the atom feed doesn't
+              if (h.kind && !existing.workerKind) { existing.workerKind = h.kind; existing.workerFlags = h.flags || null; existing.workerPct = h.pct != null ? h.pct : null; }
             }
           }
           historyMeta = hist.backfill || null;
@@ -2400,9 +2429,18 @@ async function loadSchedule13() {
     sched13Filings.forEach(r => { r.activist = sched13Activist(r); });
     applySched13Memory(); // must run before tiering — escalation sets tier 4
     sched13Filings.forEach(r => { r.tier = sched13Tier(r); });
+    // Apply what's already known about each filing (local cache from a
+    // prior read, or the Worker's stored classification) BEFORE anything
+    // renders — a known financing 13D must never flash up as a card.
+    sched13Filings.forEach(r => {
+      const cached = sched13Class[r.acc];
+      if (cached) applySched13Analysis(r, cached);
+      else if (r.workerKind) applySched13Analysis(r, { kind: r.workerKind, flags: r.workerFlags || [], pct: r.workerPct != null ? r.workerPct : null });
+    });
     if (!sched13Filings.length) throw new Error('No 13D/13G filings returned');
     setFreshness('sched13', '13D/G', 'live', 'filed within days of the trade');
     renderSched13();
+    analyzeOpportunitySched13(); // read every fresh tier-2+ filing's text in the background
     recordCampaignEvents(sched13Filings
       .filter(r => r.tier >= 2)
       .map(r => ({
@@ -2441,7 +2479,7 @@ function renderSched13() {
     <div class="status-box" style="margin-bottom:16px;text-align:left;padding:16px 20px">
       Live <strong>Schedule 13D / 13G</strong> filings — the disclosure required within days of any investor crossing 5% ownership of a public company.
       A <strong>NEW 13D</strong> is the classic activist opening move; <strong>13D AMEND</strong> means an existing holder changed their position or intentions; 13G is the same threshold with passive intent.
-      Rows highlighted in green are the likeliest opportunities: a new 13D, or any 13D activity from a <strong>★ known activist</strong> — most other raw 13D filings are founders and family holders, not campaigns. "★ Opportunities" shows only those. Stake % is parsed from each filing as it loads. Not investment advice — a 13D marks the <em>start</em> of a campaign, not a guaranteed outcome.
+      Rows highlighted in green are the likeliest opportunities: a new 13D, or any 13D activity from a <strong>★ known activist</strong> — most other raw 13D filings are founders and family holders, not campaigns. "★ Opportunities" shows only those. Stake % and the <strong>Read</strong> column are parsed from each filing's text as it loads: a 13D whose shares came from a securities purchase agreement, convertible or warrant package (usually with a 9.99% ownership blocker) is flagged <strong style="color:var(--rose)">FINANCING / DILUTION</strong> — that is the company selling paper to the filer, not the filer buying stock, and it is excluded from ★ Opportunities and the Confluence Board. Stated activist intent and open-market buying rank up; director/founder filers rank down. Not investment advice — a 13D marks the <em>start</em> of a campaign, not a guaranteed outcome.
       <div class="empty-note" style="margin-top:8px">${sched13HistoryMeta
         ? (sched13HistoryMeta.done
           ? 'Depth: the live feed plus the Worker\'s stored scan history and a 12-month backfill of roster-activist 13Ds from EDGAR full-text search — ★ Opportunities goes back months, not hours.'
@@ -2450,15 +2488,16 @@ function renderSched13() {
     </div>
     ${rows.length ? `
     <table class="data-table">
-      <thead><tr><th>Filed</th><th>Type</th><th>Target Company</th><th>Filed By</th><th>Stake</th><th></th></tr></thead>
+      <thead><tr><th>Filed</th><th>Type</th><th>Target Company</th><th>Filed By</th><th>Stake</th><th title="What the filing text says about HOW the shares were acquired and WHAT the filer intends — the metric that separates a real accumulation from a dilutive financing or a founder's routine filing">Read</th><th></th></tr></thead>
       <tbody>
         ${rows.map(r => `
-          <tr${r.tier >= 2 ? ' style="background:rgba(57,255,143,0.05)"' : ''}>
+          <tr${r.tier >= 2 ? ' style="background:rgba(57,255,143,0.05)"' : r.kind === 'financing' ? ' style="background:rgba(255,80,110,0.05)"' : ''}>
             <td class="mono" style="color:var(--text-muted);white-space:nowrap">${r.filed ? new Date(r.filed).toLocaleString() : '—'}</td>
             <td style="white-space:nowrap">${sched13Badge(r.form)}${r.escalation ? ' <span class="badge-pill badge-esc" title="This filer previously held this company passively (13G) and has now filed an ACTIVE 13D — the classic passive-turns-hostile escalation">13G→13D</span>' : ''}${r.activist ? ` <span class="badge-pill" style="background:var(--teal-dim);color:var(--teal)" title="Filer matches the known-activist roster">★ ${escHtml(r.activist.toUpperCase())}</span>` : ''}</td>
             <td style="font-weight:600">${escHtml(r.subject || '—')}${r.ticker ? ' · ' + tickerLink(r.ticker, r.subject) : ''}</td>
             <td style="color:var(--text-dim)">${escHtml(r.filers.join(', ') || '—')}</td>
-            <td class="num mono" id="s13pct-${r.acc}" style="color:var(--amber)">${r.form.startsWith('SCHEDULE 13D') ? '…' : '—'}</td>
+            <td class="num mono" id="s13pct-${r.acc}" style="color:var(--amber)">${r.pct != null ? r.pct.toFixed(1) + '%' : r.form.startsWith('SCHEDULE 13D') ? '…' : '—'}</td>
+            <td id="s13kind-${r.acc}" style="white-space:nowrap">${r.form.startsWith('SCHEDULE 13D') ? sched13KindHtml(r) : '<span style="color:var(--text-muted)">passive</span>'}</td>
             <td style="white-space:nowrap"><a href="${escHtml(r.link)}" target="_blank" rel="noopener" class="fund-link">Filing →</a>${sched13FundLink(r) ? ` <a href="${sched13FundLink(r)}" class="fund-link" title="This filer is on your Hedge Funds tab — see their full quarterly book">13F book →</a>` : ''}</td>
           </tr>`).join('')}
       </tbody>
@@ -2467,57 +2506,211 @@ function renderSched13() {
   enrichSched13Percents(rows);
 }
 
-// Lazily pull each displayed 13D filing and extract the disclosed ownership
-// percentage. New-style 13D/G filings are structured XML inside the full
-// submission text, so a tolerant scan for percent-of-class fields works;
-// a prose fallback covers older formats. Cached per accession, capped per
-// render to stay polite to SEC.
-const sched13PctCache = {};
+// ── WHAT KIND OF 13D IS IT? ──────────────────────────────────
+// "A 13D was filed" is not a signal. The metric that matters is HOW the
+// filer came to own the shares and WHAT they say they'll do — and the
+// filing itself tells you. The failure case this fixes (Expion Energy,
+// Aug 2026): a 13D from a fund run by the company's own interim chairman,
+// whose "9.99% stake" was a contractual blocker on a convertible debenture
+// + warrant package with a $91M further-investment right — a dilutive
+// insider financing that the old metric scored exactly like an activist
+// accumulating stock. Classification, in priority order:
+//   financing    — shares from a securities purchase / convertible /
+//                  warrant / PIPE; ownership blocker language. NOT a
+//                  buy signal; excluded from the board and demoted out
+//                  of ★ Opportunities.
+//   deal         — voting/support/merger agreement: an acquirer's 13D.
+//   activist     — stated intent: nominations, proxy, strategic review,
+//                  sale of the company, board change (beyond boilerplate).
+//   accumulation — Item 5(c) open-market purchases, no red flags.
+//   affiliate    — filer is a director/officer/founder/family holder.
+//   plain        — nothing distinctive in the text.
+const SCHED13_CLASS_KEY = 'sd_sched13_class_v2';
+const sched13Class = sdMemGet(SCHED13_CLASS_KEY, {});
+const SCHED13_KIND_MULT = { financing: 0, deal: 0.8, activist: 1.5, accumulation: 1.25, affiliate: 0.5, plain: 1, unknown: 1 };
+const SCHED13_KIND_LABEL = {
+  financing: '⚠ FINANCING / DILUTION', deal: '🤝 DEAL-LINKED', activist: '★ ACTIVIST INTENT',
+  accumulation: '📈 OPEN-MARKET BUYING', affiliate: 'AFFILIATE / INSIDER', plain: 'PLAIN 13D', unknown: '…'
+};
+
+function classify13D(text) {
+  const t = String(text || '');
+  const count = re => (t.match(re) || []).length;
+  const flags = [];
+  const financingHits =
+    count(/securities purchase agreement/gi) + count(/subscription agreement/gi) + count(/private placement/gi) +
+    count(/\bPIPE\b/g) + count(/convertible (?:note|notes|debenture|debentures|preferred)/gi) + count(/\bdebentures?\b/gi) +
+    count(/purchase warrants?\b/gi) + count(/registration rights agreement/gi) + count(/additional investment right/gi) +
+    count(/conversion price/gi) + count(/stated value/gi) + count(/exercise price/gi);
+  const blocker = /beneficial ownership (?:limitation|restriction|cap|blocker)/i.test(t) || /\b[49]\.99\s*%/.test(t) || /ownership (?:limitation|restriction) of [49]\.99/i.test(t);
+  if (blocker) flags.push('9.99%-style ownership blocker');
+  if (/securities purchase agreement|subscription agreement|private placement/i.test(t)) flags.push('shares issued by the company (SPA / private placement)');
+  if (/convertible|debenture/i.test(t)) flags.push('convertible security');
+  if (/\bwarrants?\b/i.test(t) && financingHits >= 2) flags.push('warrants');
+  if (/additional investment right/i.test(t)) flags.push('further-investment right');
+  const dealHits = count(/voting agreement/gi) + count(/support agreement/gi) + count(/agreement and plan of merger/gi) + count(/merger agreement/gi) + count(/tender offer/gi);
+  const intentHits = count(/nominat(?:e|ion|ing|ed)/gi) + count(/proxy (?:contest|solicitation|fight|statement)/gi) +
+    count(/strategic (?:alternatives|review)/gi) + count(/sale of the (?:issuer|company)/gi) + count(/board (?:composition|representation|refresh|seats?)/gi) +
+    count(/letter to the (?:board|issuer|company)/gi) + count(/maximiz(?:e|ing) (?:shareholder|stockholder) value/gi) + count(/undervalued/gi) +
+    count(/unsolicited/gi) + count(/proposal to acquire/gi) + count(/going.private/gi) + count(/special meeting/gi);
+  const openMarketHits = count(/open[- ]market/gi);
+  const affiliate = /(?:director|officer|chairman|chief executive officer|founder|president) of the (?:issuer|company)/i.test(t) ||
+    /serves? as (?:a |the )?(?:director|chairman|chief executive)/i.test(t) || /employment agreement/i.test(t);
+  if (affiliate) flags.push('filer is a director/officer/founder of the issuer');
+  if (dealHits) flags.push('voting/merger agreement');
+  if (intentHits >= 2) flags.push('stated activist intent');
+  if (openMarketHits) flags.push('open-market purchases');
+  const strongFinancing = /securities purchase agreement|subscription agreement|private placement/i.test(t) && /convertible|debenture|warrant|preferred/i.test(t);
+  let kind = 'plain';
+  if (strongFinancing || financingHits >= 4 || (blocker && financingHits >= 1)) kind = 'financing';
+  else if (dealHits >= 2) kind = 'deal';
+  else if (intentHits >= 2) kind = 'activist';
+  else if (openMarketHits >= 1 && !affiliate) kind = 'accumulation';
+  else if (affiliate || blocker) kind = 'affiliate';
+  return { kind, flags, blocker, affiliate, openMarket: openMarketHits > 0, intent: intentHits >= 2 };
+}
+
+// Parse the cover page: percent of class + aggregate shares owned. Together
+// they imply the share count, which × last close gives a market cap even
+// for names the fundamentals layer has never seen — the microcap tell.
+function parse13DCover(text) {
+  let pct = null, amt = null;
+  const tagHits = [...text.matchAll(/<[^>/]*[Pp]ercent[^>]*>\s*([\d.]+)/g)]
+    .map(x => parseFloat(x[1])).filter(v => v > 0 && v <= 100);
+  if (tagHits.length) pct = Math.max(...tagHits);
+  if (pct === null) {
+    const pm = text.match(/[Pp]ercent of class[\s\S]{0,300}?([\d.]+)\s*%/);
+    if (pm) pct = parseFloat(pm[1]);
+  }
+  const amtHits = [...text.matchAll(/<[^>/]*[Aa]ggregate[Aa]mount[^>]*>\s*([\d,]+)/g)]
+    .map(x => parseFloat(x[1].replace(/,/g, ''))).filter(v => v > 0);
+  if (amtHits.length) amt = Math.max(...amtHits);
+  if (amt === null) {
+    const am = text.match(/[Aa]ggregate amount beneficially owned[\s\S]{0,300}?([\d,]{3,})/);
+    if (am) amt = parseFloat(am[1].replace(/,/g, ''));
+  }
+  return { pct, amt, sharesOut: (pct > 0 && amt > 0) ? amt / (pct / 100) : null };
+}
+
+function applySched13Analysis(r, a) {
+  if (!a) return;
+  if (a.pct != null) {
+    r.pct = a.pct;
+    if (r.prevPct != null && Math.abs(a.pct - r.prevPct) >= 0.1) r.pctDelta = a.pct - r.prevPct;
+    for (const k of sched13HistKeys(r)) if (sched13Hist[k]) sched13Hist[k].pct = a.pct;
+  }
+  r.kind = a.kind || 'unknown';
+  r.flags = a.flags || [];
+  r.sharesOut = a.sharesOut || null;
+  if (a.mcap > 0) r.mcap = a.mcap;
+  // A financing 13D is context, not an opportunity — drop it out of the
+  // ★ view and the alert tier regardless of how it was tiered on name alone.
+  if (r.kind === 'financing' && r.tier >= 2) { r.tierBefore = r.tier; r.tier = 1; }
+}
+
+// Fetch + analyze one filing (cached per accession in localStorage so a
+// reload never re-reads EDGAR for the same filing).
+async function analyzeSched13(r) {
+  if (!r || !r.form.startsWith('SCHEDULE 13D')) return null;
+  const cached = sched13Class[r.acc];
+  if (cached) { applySched13Analysis(r, cached); return cached; }
+  const m = r.link.match(/\/data\/(\d+)\/(\d{18})\//);
+  if (!m) return null;
+  const dashed = `${m[2].slice(0, 10)}-${m[2].slice(10, 12)}-${m[2].slice(12)}`;
+  let a;
+  try {
+    const res = await fetchDirect(`https://www.sec.gov/Archives/edgar/data/${parseInt(m[1], 10)}/${m[2]}/${dashed}.txt`);
+    const text = await res.text();
+    const cover = parse13DCover(text);
+    const cls = classify13D(text);
+    a = { pct: cover.pct, amt: cover.amt, sharesOut: cover.sharesOut, kind: cls.kind, flags: cls.flags, blocker: cls.blocker, at: Date.now() };
+  } catch (e) {
+    a = { pct: null, kind: 'unknown', flags: [], at: Date.now(), failed: true };
+  }
+  if (!a.failed) {
+    sched13Class[r.acc] = a;
+    const keys = Object.keys(sched13Class);
+    if (keys.length > 600) keys.sort((x, y) => sched13Class[x].at - sched13Class[y].at).slice(0, keys.length - 600).forEach(k => delete sched13Class[k]);
+    sdMemSet(SCHED13_CLASS_KEY, sched13Class);
+    sdMemSet(SCHED13_HIST_KEY, sched13Hist);
+  }
+  applySched13Analysis(r, a);
+  // Market cap from the filing's implied share count × last close — async,
+  // optional, and only when we know the ticker.
+  if (a.sharesOut && isRealTicker(r.ticker) && !r.mcap) {
+    fetchPriceContext(r.ticker).then(ctx => {
+      if (ctx && ctx.last > 0) {
+        r.mcap = a.mcap = ctx.last * a.sharesOut;
+        if (sched13Class[r.acc]) { sched13Class[r.acc].mcap = r.mcap; sdMemSet(SCHED13_CLASS_KEY, sched13Class); }
+        renderCommandCenter();
+      }
+    });
+  }
+  return a;
+}
+
+// Quality multiplier for the confluence board + a one-line reason.
+function sched13Quality(r) {
+  const kind = r.kind || 'unknown';
+  let mult = SCHED13_KIND_MULT[kind] != null ? SCHED13_KIND_MULT[kind] : 1;
+  if (kind === 'activist' && r.flags && r.flags.includes('open-market purchases')) mult = 1.75;
+  if (r.mcap > 0 && r.mcap < 50e6 && mult > 0) mult *= 0.7; // sub-$50M names: thin float, hard to trade
+  const note = kind === 'financing' ? 'financing, not accumulation'
+    : kind === 'activist' ? 'stated activist intent in Item 4'
+    : kind === 'accumulation' ? 'open-market purchases in Item 5(c)'
+    : kind === 'affiliate' ? 'insider-affiliated filer'
+    : kind === 'deal' ? 'voting/merger agreement' : '';
+  return { mult, note };
+}
+
+function sched13KindHtml(r) {
+  const kind = r.kind || 'unknown';
+  const color = kind === 'financing' ? 'var(--rose)' : (kind === 'activist' || kind === 'accumulation') ? 'var(--teal)' : kind === 'unknown' ? 'var(--text-muted)' : 'var(--text-dim)';
+  const title = (r.flags && r.flags.length ? r.flags.join(' · ') : 'Read from the filing text as it loads') + (r.mcap > 0 ? ` · ~${fmtCap(r.mcap)} market cap` : '');
+  const micro = r.mcap > 0 && r.mcap < 50e6 ? ' <span class="badge-pill" style="background:var(--amber-dim);color:var(--amber)" title="Market cap implied by the filing\'s share count × last close">MICROCAP</span>' : '';
+  return `<span class="badge-pill" style="color:${color};border:1px solid var(--border);background:transparent" title="${escHtml(title)}">${SCHED13_KIND_LABEL[kind] || kind}</span>${micro}`;
+}
+
+// Displayed rows: analyze (cached or fetched) and fill the Stake + Read cells.
 async function enrichSched13Percents(rows) {
   const targets = rows.filter(r => r.form.startsWith('SCHEDULE 13D')).slice(0, 20);
   for (const r of targets) {
     const cell = document.getElementById('s13pct-' + r.acc);
     if (!cell) continue;
-    if (r.acc in sched13PctCache) { cell.textContent = sched13PctCache[r.acc] || '—'; continue; }
-    const m = r.link.match(/\/data\/(\d+)\/(\d{18})\//);
-    if (!m) { cell.textContent = '—'; continue; }
-    const dashed = `${m[2].slice(0, 10)}-${m[2].slice(10, 12)}-${m[2].slice(12)}`;
-    try {
-      const res = await fetchDirect(`https://www.sec.gov/Archives/edgar/data/${parseInt(m[1], 10)}/${m[2]}/${dashed}.txt`);
-      const text = await res.text();
-      let pct = null;
-      const tagHits = [...text.matchAll(/<[^>/]*[Pp]ercent[^>]*>\s*([\d.]+)/g)]
-        .map(x => parseFloat(x[1])).filter(v => v > 0 && v <= 100);
-      if (tagHits.length) pct = Math.max(...tagHits);
-      if (pct === null) {
-        const pm = text.match(/[Pp]ercent of class[\s\S]{0,300}?([\d.]+)\s*%/);
-        if (pm) pct = parseFloat(pm[1]);
-      }
-      sched13PctCache[r.acc] = pct != null ? pct.toFixed(1) + '%' : null;
-      // Remember the parsed % per (company, filer) so the NEXT amendment
-      // shows a delta — and show one now if a prior % is already on record.
-      if (pct != null) {
-        r.pct = pct;
-        if (r.prevPct != null && Math.abs(pct - r.prevPct) >= 0.1) r.pctDelta = pct - r.prevPct;
-        for (const k of sched13HistKeys(r)) {
-          if (sched13Hist[k]) sched13Hist[k].pct = pct;
-        }
-        sdMemSet(SCHED13_HIST_KEY, sched13Hist);
-      }
-    } catch (e) {
-      sched13PctCache[r.acc] = null;
-    }
+    await analyzeSched13(r);
     const c = document.getElementById('s13pct-' + r.acc);
     if (c) {
       const deltaHtml = r.pctDelta != null
         ? `<br><span class="${r.pctDelta > 0 ? 'pct-delta-up' : 'pct-delta-down'}" title="Change vs the prior filing by this filer on this company (from local memory)">${r.pctDelta > 0 ? '▲ +' : '▼ '}${r.pctDelta.toFixed(1)}pt</span>`
         : '';
-      c.innerHTML = (sched13PctCache[r.acc] || '—') + deltaHtml;
+      c.innerHTML = (r.pct != null ? r.pct.toFixed(1) + '%' : '—') + deltaHtml;
     }
+    const k = document.getElementById('s13kind-' + r.acc);
+    if (k) k.innerHTML = sched13KindHtml(r);
   }
   renderCommandCenter();
 }
 
+// Every tier-2+ filing young enough to hold a confluence card gets read —
+// not just the 20 rows on screen — so a financing 13D can never earn a
+// board card just because it wasn't visible in the table. Sequential and
+// capped to stay polite to EDGAR; cached results cost nothing.
+let sched13AnalysisRunning = false;
+async function analyzeOpportunitySched13() {
+  if (sched13AnalysisRunning) return;
+  sched13AnalysisRunning = true;
+  try {
+    const cutoff = Date.now() - 21 * 864e5;
+    const pending = sched13Filings.filter(r => r.tier >= 2 && !r.kind && r.form.startsWith('SCHEDULE 13D') && r.filed && new Date(r.filed).getTime() >= cutoff).slice(0, 40);
+    let n = 0;
+    for (const r of pending) {
+      await analyzeSched13(r);
+      if (++n % 5 === 0) renderCommandCenter();
+    }
+    if (pending.length) { renderSched13(); renderCommandCenter(); }
+  } finally { sched13AnalysisRunning = false; }
+}
+
 // ══════════════════════════════════════════════════════════════
 // COMBINED SIGNALS VIEW — ranked by disclosed dollar value
 // ══════════════════════════════════════════════════════════════
@@ -2763,6 +2956,44 @@ function valuationBoost(s) {
   return 0.85;
 }
 
+// ── MATERIALITY — the trade relative to the COMPANY, not raw dollars ──
+// Ranking by dollar value alone is the wrong metric: it hands the top of
+// the board to whoever trades at the largest company. A $5M insider buy at
+// a $3T megacap is 0.0002% of the equity — noise. A $300K CEO buy in a $60M
+// microcap is 0.5% of the whole company — that is the pattern the Form 4
+// literature actually finds predictive (trade size relative to market cap
+// and to the insider's own holdings). So every Top Signals row is scaled by
+// the share of market cap it represents. Anchor: a trade equal to 2 bp of
+// market cap scores 1×; 32 bp+ gets the 4× cap; sub-0.02 bp hits the 0.3×
+// floor. Unknown market cap → 1× (never penalize a name we can't measure).
+const signalMarketCap = {};       // ticker -> market cap USD | null
+const MATERIALITY_ANCHOR = 2e-4;  // 2 bp of market cap = neutral
+function materialityRatio(s) {
+  if (!s.ticker || !(s.valueUsd > 0)) return null;
+  const cap = signalMarketCap[s.ticker];
+  if (!(cap > 0)) return null;
+  return s.valueUsd / cap;
+}
+function materialityMult(s, lo = 0.3, hi = 4) {
+  const r = materialityRatio(s);
+  if (r == null) return 1;
+  return Math.min(hi, Math.max(lo, Math.sqrt(r / MATERIALITY_ANCHOR)));
+}
+function fmtCap(cap) {
+  if (!(cap > 0)) return '';
+  return cap >= 1e12 ? '$' + (cap / 1e12).toFixed(1) + 'T' : cap >= 1e9 ? '$' + (cap / 1e9).toFixed(1) + 'B' : '$' + (cap / 1e6).toFixed(0) + 'M';
+}
+function materialityChipHtml(s) {
+  const r = materialityRatio(s);
+  if (r == null) return '';
+  const cap = signalMarketCap[s.ticker];
+  const pct = r * 100;
+  const label = pct >= 0.1 ? pct.toFixed(2) + '%' : (r * 1e4).toFixed(2) + ' bp';
+  const color = r >= 1e-3 ? 'var(--teal)' : r < 5e-5 ? 'var(--text-muted)' : 'var(--amber)';
+  const micro = cap < 50e6 ? ' · MICROCAP' : cap < 300e6 ? ' · small cap' : '';
+  return `<span title="This trade as a share of the company's market cap (cap from SEC XBRL shares × last close). Ranking scales with this — a small trade in a small company outranks a big trade in a huge one." style="font-family:'IBM Plex Mono',monospace;font-size:10px;font-weight:700;color:${color};border:1px solid var(--border);border-radius:4px;padding:1px 6px">${label} of ${fmtCap(cap)} cap${micro}</span>`;
+}
+
 function valuationChipHtml(t) {
   const m = signalValuation[t];
   if (m == null) return '';   // unresolved or not computable — stay quiet
@@ -2772,15 +3003,20 @@ function valuationChipHtml(t) {
 
 function enrichSignalValuations(ranked) {
   if (!window.SDFundamentals || !ALPACA_PROXY_URL) return;
-  const tickers = [...new Set(ranked.filter(x => x.ticker && x.direction === 'buy').map(x => x.ticker))];
+  // Every ticker on the board, buys AND sells — market cap drives the
+  // materiality scaling for both directions; EV/EBITDA is only used on buys.
+  const tickers = [...new Set(ranked.filter(x => x.ticker).map(x => x.ticker))];
   for (const t of tickers) {
     if (t in signalValuation || valuationPending[t]) continue;
     valuationPending[t] = true;
     valuationInFlight++;
     SDFundamentals.fetchLatestClose(t)
       .then(px => SDFundamentals.getMetrics(t, px))
-      .then(m => { signalValuation[t] = (m && m.evEbitda != null) ? m.evEbitda : null; })
-      .catch(() => { signalValuation[t] = null; })
+      .then(m => {
+        signalValuation[t] = (m && m.evEbitda != null) ? m.evEbitda : null;
+        signalMarketCap[t] = (m && m.marketCap > 0) ? m.marketCap : null;
+      })
+      .catch(() => { signalValuation[t] = null; signalMarketCap[t] = null; })
       .then(() => {
         delete valuationPending[t];
         valuationInFlight--; valuationDirty = true;
@@ -2804,8 +3040,11 @@ function renderSignals() {
     .map(s => ({ ...s, ageDays: signalAgeDays(s) }))
     .filter(s => s.ageDays <= SIGNAL_MAX_AGE_DAYS)
     // sortValue carries the quality weights (insider role, cluster premium,
-    // first-buy bonus); valueUsd stays the honest displayed dollar figure.
-    .map(s => ({ ...s, score: (s.sortValue || s.valueUsd) * Math.pow(0.5, s.ageDays / SIGNAL_HALF_LIFE_DAYS) * valuationBoost(s) * contractBoost(s) }))
+    // first-buy bonus, stake-change size); valueUsd stays the honest
+    // displayed dollar figure. materialityMult() rescales by the trade's
+    // share of the company's market cap — the metric that actually ranks
+    // conviction, instead of raw dollars that just rank company size.
+    .map(s => ({ ...s, score: (s.sortValue || s.valueUsd) * materialityMult(s) * Math.pow(0.5, s.ageDays / SIGNAL_HALF_LIFE_DAYS) * valuationBoost(s) * contractBoost(s) }))
     .sort((a, b) => b.score - a.score)
     .slice(0, 25);
   renderCommandCenter();
@@ -2817,7 +3056,7 @@ function renderSignals() {
   }
   body.innerHTML = `
     <div class="status-box" style="text-align:left;padding:14px 18px;font-size:11px">
-      Conviction events, ranked by size <em>weighted by freshness</em> — recent rises, old fades, and anything past ${SIGNAL_MAX_AGE_DAYS} days drops off. The board <strong>remembers</strong>: signals persist in this browser across visits, so it builds a record over weeks instead of resetting to whatever the feeds happen to show today. 13F entries are held to a conviction bar — only NEW stakes and full EXITs that are ≥ $25M <em>and</em> ≥ 0.75% of that fund's own book (quant funds excluded entirely); routine adds/trims stay on the Hedge Funds cards where they belong. Insider entries are open-market buys and sells only (grants, RSU vesting and other compensation excluded). Duplicate joint filings are shown once — expand "filed ×N" to see each one. Buy-side rows carry a live <strong>EV/EBITDA</strong> chip computed from SEC XBRL — cheap multiples (&lt;12×) get a modest rank boost and rich ones (&gt;25×) a trim, since insider buying has historically been most informative in cheap names. Buy-side rows also get a <strong>CONTRACT 8-Ks</strong> badge when EDGAR full-text search finds recent filings matching contract/award language — a keyword lead worth reading, not a verified win — with a small extra rank boost, because committed capital plus a growing order book is the strongest overlap the public record offers.
+      Conviction events, ranked by <strong>materiality</strong> — each trade's size <em>relative to the company's market cap</em> (2 bp of the equity is neutral; 0.3% of a company scores 4× a rounding-error trade at a megacap) and, for insiders, relative to their own prior holdings (doubling a stake earns a bonus) — then <em>weighted by freshness</em>: recent rises, old fades, and anything past ${SIGNAL_MAX_AGE_DAYS} days drops off. Raw dollars are shown but no longer rank: a $5M buy at a $3T company is not a signal, a $300K CEO buy in a $60M company is. The board <strong>remembers</strong>: signals persist in this browser across visits, so it builds a record over weeks instead of resetting to whatever the feeds happen to show today. 13F entries are held to a conviction bar — only NEW stakes and full EXITs that are ≥ $25M <em>and</em> ≥ 0.75% of that fund's own book (quant funds excluded entirely); routine adds/trims stay on the Hedge Funds cards where they belong. Insider entries are open-market buys and sells only (grants, RSU vesting and other compensation excluded). Duplicate joint filings are shown once — expand "filed ×N" to see each one. Buy-side rows carry a live <strong>EV/EBITDA</strong> chip computed from SEC XBRL — cheap multiples (&lt;12×) get a modest rank boost and rich ones (&gt;25×) a trim, since insider buying has historically been most informative in cheap names. Buy-side rows also get a <strong>CONTRACT 8-Ks</strong> badge when EDGAR full-text search finds recent filings matching contract/award language — a keyword lead worth reading, not a verified win — with a small extra rank boost, because committed capital plus a growing order book is the strongest overlap the public record offers.
     </div>
     ${ranked.map((s, i) => `
     <div class="signal-row" style="grid-template-columns:auto 1fr auto auto auto">
@@ -2826,7 +3065,7 @@ function renderSignals() {
         <div class="signal-entity">${escHtml(s.entity)}</div>
         <div class="signal-detail">${escHtml(s.detail)}</div>
         ${s.context ? `<div class="ctx-note" style="margin-top:4px">↳ ${escHtml(s.context)}</div>` : ''}
-        ${s.ticker && s.direction === 'buy' && (valuationChipHtml(s.ticker) || contractChipHtml(s.ticker)) ? `<div style="margin-top:5px;display:flex;gap:6px;flex-wrap:wrap">${valuationChipHtml(s.ticker)}${contractChipHtml(s.ticker)}</div>` : ''}
+        ${s.ticker && (materialityChipHtml(s) || (s.direction === 'buy' && (valuationChipHtml(s.ticker) || contractChipHtml(s.ticker)))) ? `<div style="margin-top:5px;display:flex;gap:6px;flex-wrap:wrap">${materialityChipHtml(s)}${s.direction === 'buy' ? valuationChipHtml(s.ticker) + contractChipHtml(s.ticker) : ''}</div>` : ''}
         ${s.count > 1 ? `
         <div style="margin-top:6px">
           <button class="chart-jump-btn" data-closed-label="filed ×${s.count} — show all ▾" onclick="toggleSigFilings('sig-filings-${i}', this)">filed ×${s.count} — show all ▾</button>
